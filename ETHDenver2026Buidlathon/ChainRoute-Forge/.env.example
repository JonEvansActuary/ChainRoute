# ChainRoute-Forge – optional env (copy to .env and fill as needed)
# All keys are optional; without Arweave keys, support/blob post will return 503.

# RPC (optional – use provider URLs for better reliability and higher rate limits)
# NEXT_PUBLIC_AMOY_RPC=          # e.g. https://polygon-amoy.g.alchemy.com/v2/YOUR_KEY or Infura/QuickNode Amoy URL
# NEXT_PUBLIC_POLYGON_MAINNET_RPC= # e.g. https://polygon-mainnet.g.alchemy.com/v2/YOUR_KEY (for Load Example Chain)

# Arweave read (optional – gateway + GraphQL for fetching blobs and tags; improves reliability)
# NEXT_PUBLIC_ARWEAVE_GATEWAY=   # e.g. https://arweave.net or another gateway URL
# NEXT_PUBLIC_ARWEAVE_GRAPHQL=   # e.g. https://arweave.net/graphql (for tag lookups)

# AI (caption + event suggestions)
# OPENAI_API_KEY=
# GROK_API_KEY=

# Arweave (server-side post for supports and blobs)
# ARWEAVE_KEY_PATH=   # path to JWK file
# ARWEAVE_JWK=        # or inline JSON (string)
